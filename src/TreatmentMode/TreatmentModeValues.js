const formValues = {
    "edit_title": "Editar Modalidad de tratamientos",
    "new_title": "Nueva Modalidad de tratamientos",
    "fields": [
        {
            "component": "text",
            "label": "Nombre",
            "type": "text",
            "name": "name",
            "required": true,
        }
    ]
}

const listValues = {
    "title": "Modalidades de tratamientos",
    "new_title": "Nueva Modalidad de tratamientos",
    "fields": [
        {
            "label": "Nombre",
            "name": "name",
        }
    ]
}

const detailValues = {
    "title": "Modalidad de tratamientos",
    "fields": [
        {
            "label": "Nombre",
            "name": "name",
        }
    ]
}

export default {
    formValues,
    listValues,
    detailValues
};